import 'package:flutter/material.dart';

class Myhp1 extends StatelessWidget {
  const Myhp1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Home Page"),
      ),
    );
  }
}
