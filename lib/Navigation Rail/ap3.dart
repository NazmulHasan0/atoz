import 'package:flutter/material.dart';

class Myap3 extends StatelessWidget {
  const Myap3({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Add Page"),
      ),
    );
  }
}
