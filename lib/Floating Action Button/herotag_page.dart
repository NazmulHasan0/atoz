import 'package:flutter/material.dart';

class Myherotag extends StatelessWidget {
  const Myherotag({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("This is hero Tag Page"),
      ),
    );
  }
}
